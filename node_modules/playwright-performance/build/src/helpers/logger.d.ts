export default class Logger {
    private _suppressConsoleResults;
    constructor(suppressConsoleResults: boolean);
    info(message: string | object, isMandatory: boolean, isTable?: boolean): void;
    error(message: string | object, isMandatory: boolean): void;
}
