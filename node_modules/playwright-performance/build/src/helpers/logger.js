"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Logger {
    constructor(suppressConsoleResults) {
        this._suppressConsoleResults = suppressConsoleResults;
    }
    info(message, isMandatory, isTable = false) {
        if (!this._suppressConsoleResults || isMandatory) {
            if (!isTable)
                console.log(message);
            else
                console.table(message);
        }
    }
    error(message, isMandatory) {
        if (!this._suppressConsoleResults || isMandatory) {
            console.error(`Playwright-Performance error: ${message}`);
        }
    }
}
exports.default = Logger;
